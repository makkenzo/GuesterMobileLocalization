<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Guester.Tamplates.ButtonTemplate"
             x:Name="this"
             xmlns:mcu="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             >
    

    <HorizontalStackLayout  MinimumWidthRequest="80" Padding="5,0">
        <Border Padding="5,0" BackgroundColor="Transparent">
            <Label FontAttributes="Bold" FontSize="Subtitle" HorizontalOptions="Center" VerticalOptions="Center" 
            TextColor="{AppThemeBinding Dark={StaticResource TextColorDark},Light={StaticResource TextColorLight}}" Text="{Binding Title, Source={x:Reference this}}" />
        </Border>
        <Border BackgroundColor="Red" Margin="5" Padding="5,2" HorizontalOptions="Start" VerticalOptions="Center" IsVisible="{Binding IsCounterVisible, Source={x:Reference this}, Mode=TwoWay}" >
                <Label Text="{Binding Counter, Source={x:Reference this}}" FontAttributes="Bold" VerticalOptions="Center" HorizontalOptions="Center" FontSize="Small" TextColor="White" />
        </Border>
    </HorizontalStackLayout>
</ContentView>

